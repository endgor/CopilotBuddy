<Window x:Class="CopilotBuddy.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        x:Name="Window"
        Title="CopilotBuddy"
        Icon="pack://application:,,,/Resources/bender.ico"
        Width="531"
        Height="350"
        MinWidth="350"
        MinHeight="229"
        WindowStartupLocation="Manual"
        Background="#FF595959"
        Closing="Window_Closing"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Colors matching HB 5.4.8 ExpressionDark theme -->
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#FF595959"/>
        <SolidColorBrush x:Key="NormalBrush" Color="#FF595959"/>
        <SolidColorBrush x:Key="NormalBorderBrush" Color="#FF333333"/>
        <SolidColorBrush x:Key="HoverBrush" Color="#FF393939"/>
        <SolidColorBrush x:Key="TextBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="ControlBackgroundBrush" Color="#FF333333"/>
        <SolidColorBrush x:Key="DisabledForegroundBrush" Color="#FF888888"/>
        <SolidColorBrush x:Key="GlyphBrush" Color="#FFD1D1D1"/>

        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource NormalBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource NormalBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF2A2A2A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource DisabledForegroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Toggle Button Template -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="20"/>
                </Grid.ColumnDefinitions>
                <Border x:Name="Border"
                        Grid.ColumnSpan="2"
                        Background="{StaticResource NormalBrush}"
                        BorderBrush="{StaticResource NormalBorderBrush}"
                        BorderThickness="1"/>
                <Border Grid.Column="0"
                        Margin="1"
                        Background="{StaticResource NormalBrush}"/>
                <Path x:Name="Arrow"
                      Grid.Column="1"
                      Fill="{StaticResource GlyphBrush}"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Data="M 0 0 L 4 4 L 8 0 Z"/>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Border" Property="Background" Value="{StaticResource HoverBrush}"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource NormalBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource NormalBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Template="{StaticResource ComboBoxToggleButton}"
                                          Grid.Column="2"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"/>
                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="6,3,23,3"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="{StaticResource ControlBackgroundBrush}"
                                            BorderBrush="{StaticResource NormalBorderBrush}"
                                            BorderThickness="1"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem Style - texte blanc sur fond foncé comme HB -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        </Style>

        <!-- CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <!-- TabControl Style -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource NormalBorderBrush}"/>
        </Style>

        <!-- TabItem Style -->
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="{StaticResource NormalBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource NormalBorderBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border"
                                Background="{StaticResource NormalBrush}"
                                BorderBrush="{StaticResource NormalBorderBrush}"
                                BorderThickness="1,1,1,0"
                                Padding="6,2">
                            <ContentPresenter x:Name="ContentSite"
                                              ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource NormalBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- RichTextBox Style -->
        <Style TargetType="RichTextBox">
            <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{x:Null}"/>
        </Style>

        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <!-- StatusBar Style -->
        <Style TargetType="StatusBar">
            <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}"/>
        </Style>
    </Window.Resources>

    <Grid x:Name="LayoutRoot">
        <!-- Bot Selector ComboBox -->
        <ComboBox x:Name="cmbBotSelector"
                  IsEditable="False"
                  HorizontalAlignment="Right"
                  Margin="0,8,8,0"
                  VerticalAlignment="Top"
                  Width="120"
                  SelectionChanged="cmbBotSelector_SelectionChanged"/>

        <!-- Load Profile Button -->
        <Button x:Name="btnLoadProfile"
                HorizontalAlignment="Right"
                Margin="0,33,8,0"
                VerticalAlignment="Top"
                Height="18"
                Width="120"
                Click="btnLoadProfile_Click">
            <Viewbox Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Load Profile"/>
            </Viewbox>
        </Button>

        <!-- Settings Button -->
        <Button x:Name="btnSettings"
                Height="18"
                HorizontalAlignment="Right"
                Margin="0,55.92,8,0"
                VerticalAlignment="Top"
                Width="120"
                Click="btnSettings_Click">
            <Viewbox Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Settings &amp; Tools"/>
            </Viewbox>
        </Button>

        <!-- Enhanced Mode Checkbox -->
        <CheckBox x:Name="EnhancedMode"
                  HorizontalAlignment="Right"
                  Margin="0,101,8,0"
                  VerticalAlignment="Top"
                  Width="120"
                  ToolTip="Makes all buttons visible in the main window"
                  Content="Enhanced Mode"
                  Unchecked="EnhancedMode_Unchecked"
                  Checked="EnhancedMode_Checked"/>

        <!-- Bot Config Button (hidden by default) -->
        <Button x:Name="btnBotConfig"
                Visibility="Hidden"
                Margin="0,126,8,0"
                VerticalAlignment="Top"
                Height="19"
                HorizontalAlignment="Right"
                Width="120"
                Click="btnBotConfig_Click">
            <Viewbox Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Bot Config"/>
            </Viewbox>
        </Button>

        <!-- Class Config Button (hidden by default) -->
        <Button x:Name="btnClassConfig"
                Visibility="Hidden"
                Margin="0,151,8,0"
                VerticalAlignment="Top"
                Height="19"
                HorizontalAlignment="Right"
                Width="120"
                Click="btnClassConfig_Click">
            <Viewbox Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Class Config"/>
            </Viewbox>
        </Button>

        <!-- Plugins Button (hidden by default) -->
        <Button x:Name="btnPlugins"
                Visibility="Hidden"
                Margin="0,176,8,0"
                VerticalAlignment="Top"
                Height="19"
                HorizontalAlignment="Right"
                Width="120"
                Click="btnPlugins_Click">
            <Viewbox Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Plugins"/>
            </Viewbox>
        </Button>

        <!-- Dev Tools Button (hidden by default) -->
        <Button x:Name="btnDevTools"
                Height="19"
                Visibility="Hidden"
                Margin="0,201,8,0"
                VerticalAlignment="Top"
                HorizontalAlignment="Right"
                Width="120"
                Click="btnDevTools_Click">
            <Viewbox Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Developer Tools"/>
            </Viewbox>
        </Button>

        <!-- Start Button -->
        <Button x:Name="btnStart"
                HorizontalAlignment="Right"
                Margin="0,0,8,26"
                VerticalAlignment="Bottom"
                Width="120"
                Height="25"
                Click="btnStart_Click">
            <Viewbox Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,2,0,2">
                <TextBlock Text="Start"/>
            </Viewbox>
        </Button>

        <!-- Stop Button (hidden by default) -->
        <Button x:Name="btnStop"
                Visibility="Hidden"
                HorizontalAlignment="Right"
                Margin="0,0,8,26"
                VerticalAlignment="Bottom"
                Height="25"
                Width="120"
                Click="btnStop_Click">
            <Viewbox Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,2,0,2">
                <TextBlock Text="Stop"/>
            </Viewbox>
        </Button>

        <!-- Tab Control (Log and Info tabs) -->
        <TabControl x:Name="tabControl"
                    Margin="8,8,132,25.96"
                    TabStripPlacement="Bottom">
            <!-- Log Tab -->
            <TabItem Header="Log">
                <Grid Background="#FF333333">
                    <ScrollViewer x:Name="scrollLog" VerticalScrollBarVisibility="Auto" Background="#FF333333">
                        <RichTextBox x:Name="rtbLog"
                                     Margin="0"
                                     IsReadOnly="True"
                                     UndoLimit="0"
                                     IsUndoEnabled="False"
                                     BorderThickness="0"
                                     Background="#FF333333"
                                     Foreground="#FFFFFFFF"
                                     FontFamily="Consolas"
                                     FontSize="12"
                                     VerticalScrollBarVisibility="Disabled">
                            <RichTextBox.Resources>
                                <Style TargetType="{x:Type Paragraph}">
                                    <Setter Property="Margin" Value="0"/>
                                </Style>
                            </RichTextBox.Resources>
                        </RichTextBox>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <!-- Info Tab -->
            <TabItem Header="Info">
                <Grid Background="#FF333333">
                    <ScrollViewer x:Name="scrollInfo" VerticalScrollBarVisibility="Auto" Background="#FF333333">
                        <TextBox x:Name="tbInfoBlock"
                                 Margin="5,5,5,5"
                                 BorderThickness="0"
                                 IsReadOnly="True"
                                 TextWrapping="Wrap"
                                 Background="#FF333333"
                                 Foreground="#FFFFFFFF"
                                 FontFamily="Consolas"
                                 FontSize="12"/>
                    </ScrollViewer>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <StatusBar Margin="0"
                   VerticalAlignment="Bottom"
                   Background="{StaticResource WindowBackgroundBrush}"
                   Panel.ZIndex="1">
            <StatusBarItem x:Name="lblActivityText"
                           Foreground="#FFFFFFFF"
                           Content="Ready"/>
        </StatusBar>
    </Grid>
</Window>
